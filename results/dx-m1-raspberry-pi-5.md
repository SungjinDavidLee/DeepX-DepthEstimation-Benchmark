# Raspberry Pi 5 + DEEPX DX-M1

측정일: 2026-08-10

---

## 1. 환경

| 항목 | 값 |
|---|---|
| 호스트 | Raspberry Pi 5 (8GB) |
| 가속기 | DEEPX DX-M1 (M.2 2280, PCIe Gen3 x1) |
| 카메라 | Intel RealSense D405 (FW 5.15.1.55) |
| OS | Debian GNU/Linux 13 (trixie), aarch64 |
| 커널 | 6.18.39+rpt-rpi-2712 |
| 런타임 | DX-RT (DX-AllSuite 배포판) |
| 연산 정밀도 | INT8 |

환경 구축 절차는 [../docs/setup-dx-m1.md](../docs/setup-dx-m1.md) 참조.

## 2. 측정 조건

- 입력: 카메라 실시간 스트림
- 실행 모드: sync
- 배치 크기: 1
- 렌더링: 비활성화 (`--no-display`)
- 전력 모드 / 클럭 고정: 별도 조정 없음 (기본값)
- 주변 온도: 미기록

## 3. 실행

```bash
cd dx_app/bin
./depth_anything_v2_vitb_sync \
  -m ../assets/models/depthanythingv2-vits_224x224.dxnn \
  -c <camera_index> --no-display
```

모델 파일만 교체하여 ViT-B도 동일하게 실행했다. 모델은 배포판 Model Zoo에 사전 컴파일된 `.dxnn`을 사용했으며 별도 컴파일 과정은 없다.

## 4. 결과

### 4-1. 종합

| 모델 | 입력 | 추론 지연 | 전체 FPS | 표본 |
|---|---|---|---|---|
| depthanythingv2-vits | 224×224 | 76.63 ms | 12.4 | 353 프레임 / 28.5 s |
| depthanythingv2-vitb | 224×224 | 162.37 ms | 6.0 | 3,483 프레임 / 579.4 s |

### 4-2. 구간 분해

**depthanythingv2-vits_224x224**

| 구간 | 평균 지연 | 처리량 |
|---|---|---|
| Read | 0.38 ms | 2654.0 FPS |
| Preprocess | 0.93 ms | 1077.8 FPS |
| Inference | 76.63 ms | 13.1 FPS |
| Postprocess | 0.09 ms | 11010.1 FPS |

**depthanythingv2-vitb_224x224**

| 구간 | 평균 지연 | 처리량 |
|---|---|---|
| Read | 0.32 ms | 3133.3 FPS |
| Preprocess | 0.90 ms | 1110.1 FPS |
| Inference | 162.37 ms | 6.2 FPS |
| Postprocess | 0.09 ms | 10935.3 FPS |

## 5. 관찰

### 5-1. 병목은 전적으로 추론이다

두 경우 모두 Read + Preprocess + Postprocess의 합이 1.5 ms 미만이며, 전체 지연의 98% 이상을 추론이 차지한다. 호스트 CPU는 대부분의 시간을 대기에 사용한다.

이는 sync 모드의 특성이다. 추론 완료 전까지 다음 프레임 처리가 시작되지 않으므로, async 모드에서는 결과가 달라질 수 있다. 이번 측정에는 포함하지 않았다.

### 5-2. 모델 규모가 2.1배의 차이를 만든다

ViT-S와 ViT-B는 입력 해상도가 224×224로 동일하고 전처리·후처리 비용도 사실상 같다. 따라서 76.63 ms와 162.37 ms의 차이는 모델 규모에서만 기인한다.

### 5-3. 참고 — 입력 픽셀 수와 추론 시간이 비례하지 않는다

같은 장비에서 인스턴스 분할 모델(yolov8-n-seg, 640×640)을 측정했을 때 추론 지연은 63.45 ms였다. 입력 픽셀이 약 8배 많음에도 ViT-S(224×224, 76.63 ms)보다 짧다.

**가설:** DX-M1이 convolution 계열 연산에 최적화되어 있고, ViT 기반 모델의 attention 연산은 상대적으로 메모리 대역폭 제약을 받는다.

이번 측정만으로는 확정할 수 없다. 검증하려면 동일 계열 내 해상도 변화 측정 또는 연산자 단위 프로파일링이 필요하다.

## 6. 한계

- **표본 크기가 불균등하다.** ViT-B는 579초/3,483프레임, ViT-S는 28.5초/353프레임이다. 후자는 신뢰구간이 넓다.
- **지연 분포를 알 수 없다.** 실행 파일이 평균만 출력하므로 p95·p99를 확보하지 못했다.
- **열 조건을 통제하지 않았다.** CPU governor, 주변 온도, 방열 상태를 기록하지 않았다. 장시간 실행 시 스로틀링 영향을 배제할 수 없다.
- **전력을 측정하지 않았다.** Raspberry Pi는 내장 전력 센서가 없어 외부 계측이 필요하다.
- **정확도를 측정하지 않았다.** 추정 깊이와 D405 스테레오 깊이의 대조는 수행하지 않았다.
